Place vendored AdminLTE 3 assets in this directory if you want to serve them locally.

The current templates use CDN-hosted AdminLTE 3 assets so the dashboard can run immediately without a manual download step.
